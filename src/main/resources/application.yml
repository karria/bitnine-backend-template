spring:
  config:
    activate:
      on-profile: local
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/template
    username: template
    password: template
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/template
    username: template
    password: template
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 80
---
spring:
  config:
    activate:
      on-profile: prod
server:
  port: 80
